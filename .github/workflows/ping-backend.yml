name: Ping backend on schedule

on:
  schedule:
    # 5分ごと
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call health endpoint
        run: |
          curl -sS https://chemistry-app-bm05.onrender.com/health || echo "failed"
